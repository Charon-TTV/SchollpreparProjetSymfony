{% extends 'admin/base.html.twig' %}

{% block title %}Modifier la Filière - SchoolPrepar{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row justify-content-center"> {# Centre la ligne #}
        <div class="col-md-8 col-lg-6"> {# Définit une largeur raisonnable #}
            <div class="card shadow-sm border-0 mt-4">
                <div class="card-header bg-white py-3 border-bottom-0">
                    <h5 class="mb-0 text-primary font-weight-bold">
                        <i class="feather icon-edit mr-2"></i>Modifier la Filière
                    </h5>
                </div>
                <div class="card-body">
                    {{ include('admin/admin_filiere/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
